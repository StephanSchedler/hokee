- Create output dir if it does not exist
- Add class Settings to save and load Settings, e.g. input,rules,output folder
- make editor,browser configurable 
- make csv format configurable
- make categories (e.g. ordering in html) configurable
- Linux support
- Add Github project
- include fmt as cmake subdirectory
- add fmt license
- try deepcode
- Test path of vcvarsall.bat" 
- Tests: 
    - Report generator-> save summary table as csv and check.
    - Add Test for fix rules 
    - Add Test for add rules